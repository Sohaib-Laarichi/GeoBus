package com.geobus.marrakech

import android.app.Application

/**
 * Classe principale de l'application
 * Initialise les composants globaux
 */
class GeoBusApplication : Application() {

    override fun onCreate() {
        super.onCreate()
        // Initialisation des composants globaux si nécessaire
    }
}